int minSteps(string str) {
    // Write your code here.
    int n=str.size();
    int count=0;
    for(int i=1;i<n;i++){
        if(str[i]!=str[i-1])
        count++;
    }
    return (count+1)/2+1;
}
